# docker-flask-app
A simple and containerized Flask web application built using Python, Flask, and Docker. This project demonstrates how to run a lightweight web server inside a Docker container for consistent, portable deployment across any environment. 
